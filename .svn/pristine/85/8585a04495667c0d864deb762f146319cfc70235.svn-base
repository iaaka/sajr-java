% Generated by roxygen2 (4.0.1): do not edit by hand
\name{clustSegs}
\alias{clustSegs}
\title{Cluster segments by splicing similarity}
\usage{
clustSegs(data, k, rows, cols, col, dist = "cor", norm.groups = NULL,
  norm.sd = F, outline = T, plotPCAandTree = F)
}
\arguments{
\item{data}{object of sajr class, produced by \code{\link{loadSAData}}. You may want to use only significan ones (see \code{\link{calcSAPvalue}})}

\item{k}{desired numer of clusters}

\item{rows}{number of rows in multiplot}

\item{cols}{number of columns in multiplot}

\item{col}{graphical parameter}

\item{dist}{type of distance to be used. Either cor (1-cor will be used) or argument of dist function}

\item{norm.groups}{factors that denotes in which groups mean should be normolised to 0}

\item{norm.sd}{whether variance should be normolized to 1}

\item{outline}{whether outline values shold be plotted}
}
\value{
clustering for segments. Segments with zero variance (after mean normalization) will omitted
}
\description{
Performs simple \code{\link{hcluts}} and plots clusters as boxplot. First normalize mean and variance to 0 and 1 (see norm.groups parameter) and then used 1-cor distance.
}
\examples{
t = clustSegs(age[,c(1,3,2,4)],6,2,3,col=c('orange','gray','orange','gray'),dist='cor',norm.groups=c(1,2,1,2))
}

